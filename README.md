# 🧘 ZenPulse: AI Meditation App

Прототип мобильного приложения для медитации с AI-персонализацией.

**Стек:** React Native + Expo + TypeScript

---

## 📱 Запуск приложения

```bash
# Установка зависимостей
npm install

# Запуск в LAN режиме (телефон и компьютер в одной Wi-Fi сети)
npx expo start --lan

# Или через tunnel (работает из любой сети)
npx expo start --tunnel
```

**Открытие на устройстве:**
1. Установите **Expo Go** из App Store / Google Play
2. Отсканируйте QR-код из терминала

---

## 🎯 Реализованный функционал (по ТЗ)

### Экран «Paywall» (Экран подписки)
- ✅ Яркий, «дорогой» дизайн в стиле медитаций
- ✅ Список преимуществ Premium-версии (6 пунктов с иконками)
- ✅ Два тарифа: Месячный (499₽) / Годовой (2990₽)
- ✅ Визуальное выделение выгодного тарифа (бейдж "Лучшая цена")
- ✅ Кнопка «Попробовать 7 дней бесплатно» → имитация покупки

### Экран «Медитации» (Главная)
- ✅ Карточки с изображениями и длительностью сессий
- ✅ Логика «Заблокировано»: серые карточки с замком при `isSubscribed = false`
- ✅ Клик на заблокированный контент → переход на Paywall

### AI Настрой дня (Vibe-фича)
- ✅ Выбор настроения из 3 вариантов (иконки: ☀️ Радостно, 🌤 Нейтрально, 🌧 Грустно)
- ✅ Генерация аффирмации через мок-ответ LLM
- ✅ Анимация загрузки при "запросе к AI"

---

## 📐 Mobile UX: Работа с мобильной спецификой

### SafeArea (защита от «чёлки»)

```tsx
import { SafeAreaView } from 'react-native-safe-area-context';

// Обёртка всех экранов
<SafeAreaView style={styles.safeArea} edges={['top', 'bottom']}>
  {/* Контент защищён от выреза камеры и Home Indicator */}
</SafeAreaView>
```

**Где применено:**
- `PaywallScreen.tsx` — edges: ['top', 'bottom']
- `MeditationsScreen.tsx` — edges: ['top'] (нижняя часть для скролла)

### Адаптация к разным экранам (iPhone SE vs Pro Max)

```tsx
const { width, height } = Dimensions.get('window');

// Определение типа устройства
const isSmallDevice = width < 380;      // iPhone SE, iPhone 8 (375px)
const isMediumDevice = width >= 380 && width < 414;  // iPhone 14 (390px)
const isLargeDevice = width >= 414;     // iPhone Pro Max (430px)
const isShortDevice = height < 700;     // Короткие экраны (SE)

// Адаптивные отступы
const horizontalPadding = isSmallDevice ? 16 : isLargeDevice ? 24 : 20;
```

**Адаптивные элементы:**

| Элемент | iPhone SE | iPhone 14 | Pro Max |
|---------|-----------|-----------|---------|
| Горизонтальные отступы | 16px | 20px | 24px |
| Заголовок | 24px | 28px | 30px |
| Высота карточки | 110px | 130px | 140px |
| Размер логотипа | 64px | 80px | 88px |
| Нижний padding | 80px | 100px | 100px |

### Кликабельность элементов

```tsx
// Минимальный размер тач-зоны по Apple HIG: 44x44pt
<TouchableOpacity
  style={styles.button}
  activeOpacity={0.7}  // Визуальный feedback
  hitSlop={{ top: 10, bottom: 10, left: 10, right: 10 }}  // Увеличение зоны нажатия
>
```

**Применённые практики:**
- `activeOpacity={0.7-0.8}` — визуальная обратная связь
- Все кнопки минимум 44px высотой
- Карточки медитаций — полностью кликабельные области

### Скроллинг и списки

```tsx
<ScrollView
  showsVerticalScrollIndicator={false}  // Скрытие полосы прокрутки
  bounces={true}                         // iOS bounce effect
  contentContainerStyle={styles.scrollContent}
>
```

**Отступы для скролла:**
- `paddingBottom: 100px` — контент не прячется за Home Indicator
- Последний элемент всегда доступен для просмотра

---

## 🎨 Дизайн в стиле медитации

### Цветовая палитра (по рекомендациям Purrweb)

> «Спокойные, пастельные тона помогают расслабиться»

```tsx
// Основные цвета — мягкие, не агрессивные
primary: '#6B7FD7',    // Мягкий индиго (спокойствие)
secondary: '#9B7ED9',  // Лавандовый (релаксация)
accent: '#F5C563',     // Мягкий золотистый (тепло)
success: '#5BBD8B',    // Мятный (природа, гармония)
teal: '#6BC5D2',       // Бирюзовый (вода, спокойствие)
```

### Мягкие формы (скругления)

> «Избегайте резких линий и углов»

```tsx
// Все элементы с мягкими скруглениями
borderRadius: {
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
}
```

### «Воздух» в интерфейсе

> «Внутренние и внешние поля добавляют "воздух"»

- Увеличенные отступы между секциями (28px)
- Padding в карточках (14-20px)
- Gap между элементами сетки (12px)

### Природные изображения

Все медитации используют фото природы с Unsplash:
- Восход солнца
- Океан и пляж
- Горные пейзажи
- Лесные сцены

---

## 🔐 Логика подписки

### Контекст состояния

```tsx
// src/context/SubscriptionContext.tsx
interface SubscriptionContextType {
  isSubscribed: boolean;
  subscribe: () => void;
  unsubscribe: () => void;
}
```

### Условие блокировки контента

```tsx
// Логика: если isSubscribed = false, показывай замок
const isLocked = meditation.isPremium && !isSubscribed;

if (isLocked) {
  navigation.navigate('Paywall');  // Переход на экран подписки
}
```

### Визуальное состояние «Заблокировано»

```tsx
{isLocked && (
  <View style={styles.lockedOverlay}>
    <Ionicons name="lock-closed" size={22} color="#FFF" />
    <Text>Premium</Text>
  </View>
)}

// Стили для заблокированной карточки
cardImageLocked: {
  opacity: 0.4,  // Затемнение изображения
}
```

---

## 🤖 AI Настрой дня (Prompt Engineering)

### Выбор настроения → Генерация аффирмации

```tsx
type Mood = 'happy' | 'neutral' | 'sad';

const generateAiAffirmation = async (mood: Mood) => {
  setIsLoading(true);
  
  // Имитация API запроса к LLM (1.5 сек)
  await new Promise(resolve => setTimeout(resolve, 1500));
  
  // Мок-ответы, структурированные по настроению
  const affirmation = aiAffirmations[mood][randomIndex];
  setAiResponse(affirmation);
};
```

### Пример промпта для реального LLM

```
Ты — дзен-мастер в приложении для медитации ZenPulse.
Пользователь выбрал настроение: {mood}.

Сгенерируй персональную аффирмацию на русском языке:
- 2-3 предложения
- Тёплый, поддерживающий тон
- Без эмодзи
- Фокус на осознанности и принятии

Настроения:
- happy: усиль радость, вдохнови на действия
- neutral: создай баланс, открой новые возможности
- sad: поддержи, напомни о внутренней силе
```

---

## 📁 Структура проекта

```
ZenPulse/
├── App.tsx                    # Точка входа
├── src/
│   ├── context/
│   │   └── SubscriptionContext.tsx   # Глобальное состояние подписки
│   ├── data/
│   │   └── meditations.ts     # Данные медитаций + AI аффирмации
│   ├── navigation/
│   │   └── AppNavigator.tsx   # Stack Navigator
│   ├── screens/
│   │   ├── PaywallScreen.tsx  # Экран подписки
│   │   └── MeditationsScreen.tsx  # Главный экран
│   └── theme/
│       └── colors.ts          # Цвета, градиенты, тени, spacing
```

---

## 📚 Использованные библиотеки

| Библиотека | Назначение |
|------------|------------|
| `expo` | Фреймворк для React Native |
| `@react-navigation/native` | Навигация между экранами |
| `@react-navigation/stack` | Stack Navigator |
| `react-native-safe-area-context` | SafeArea для «чёлки» |
| `expo-linear-gradient` | Градиенты |
| `@expo/vector-icons` | Иконки (Ionicons) |

---

## ✅ Чек-лист соответствия ТЗ

- [x] React Native + Expo
- [x] 2 экрана: Paywall, Медитации
- [x] SafeArea (текст не залезает на «чёлку»)
- [x] Кликабельность элементов
- [x] Яркий дизайн в стиле медитаций
- [x] Список преимуществ Premium
- [x] Два тарифа с выделением выгодного
- [x] Кнопка «Попробовать бесплатно»
- [x] Карточки медитаций с картинками и временем
- [x] Логика «Заблокировано» при isSubscribed = false
- [x] AI Настрой дня (3 настроения → аффирмация)
- [x] Адаптация к разным экранам (iPhone SE - Pro Max)

---

**Автор:** GitHub Copilot  
**Дата:** Декабрь 2025
